option_settings:
  aws:elasticbeanstalk:container:nodejs:
    ProxyServer: nginx

files:
  "/etc/nginx/conf.d/elasticbeanstalk/react_rewrites.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      location / {
        try_files $uri /index.html;
      }
