version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - echo "🔍 Printing current directory"
            - pwd
            - echo "🔍 Listing root"
            - ls -al
            - echo "🔍 Listing one level deep"
            - ls -alR | head -200
            - echo "🧹 Installing frontend deps"
            - npm install
        build:
          commands:
            - echo "🔍 During build, print structure again"
            - pwd
            - ls -al
            - ls -alR | head -200
            - echo "⛔ Stopping build intentionally"
            - exit 1
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
