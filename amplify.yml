version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - echo "📦 Installing frontend dependencies..."
            - npm install
        build:
          commands:
            - echo "⚙️ Building Vite frontend..."
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
